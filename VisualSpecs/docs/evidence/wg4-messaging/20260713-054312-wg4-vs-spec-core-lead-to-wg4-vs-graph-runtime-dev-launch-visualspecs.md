# Tarea: levantar VisualSpecs

Objetivo: iniciar la app VisualSpecs del repositorio y mantener el servidor disponible para que el coordinador la abra en el navegador del usuario.

Repositorio: `C:\Users\maria\0_repos\CodebaseConstellation_iac\.ac\wg-4-vs-dev-team\repo-CodebaseConstellation`

Alcance y restricciones:

- Verificá en el repo el comando canónico de desarrollo para `VisualSpecs`.
- Iniciá la app en segundo plano, sin modificar código ni archivos de configuración versionados.
- Comprobá por HTTP que la URL local responde correctamente.
- Evitá matar procesos ajenos; si el puerto ya está ocupado por una instancia válida de VisualSpecs, reutilizala.

Reporte esperado: URL exacta, comando usado, PID/proceso que mantiene el servidor, prueba HTTP y cualquier advertencia relevante. Respondé explícitamente al terminar o si quedás bloqueado.
