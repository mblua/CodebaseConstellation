# Issue #7 — READY_FOR_IMPLEMENTATION

Core conformance passed for plan commit `c2f45f893c03df6f456240362c22ae38ee977312` on `feature/7-collapsible-project-rail`.

I verified the exact SHA, one-file plan diff, clean tracked tree, binding UTF-16 escape algorithm, atom-safe collision token/accessibility requirements, session-kind discard-copy deduplication, explicit `1663px` Hybrid evidence flow, report dispositions, and retained executable gates. The remaining identity P1 was accepted and closed by binding core arbitration, not waived or accepted as product risk. Resilience reported `NO_BLOCKING_PREMORTEM_FINDINGS`; all other semantic P1s were cleared at plan level.

Readiness verdict: **`READY_FOR_IMPLEMENTATION`**.

You remain the single owner of the graph/runtime UI artifact. Implement the approved RFC completely on the current issue branch. Do not push, open a PR, or merge yet.

Required execution:

1. First record this readiness in the RFC (`Status: IMPLEMENTATION_IN_PROGRESS`, readiness/core-conformance record referencing `c2f45f8` and this decision), then proceed to production changes.
2. Follow the planned order: atomic lifecycle/session boundary and deferred unit harnesses; authoritative discard semantics and action-error isolation; stable mounted Project Rail and shared presentation model/handlers; exact escaped identity and accessibility; three-band layout/focus/preferences; coalesced resize/performance instrumentation; browser evidence.
3. Stay inside the allowed artifact list and explicit no-change boundaries. In particular do not change schema/contract, renderer port/Canvas2D adapter, project-store port/FSA adapter, graph semantics, persistence bytes, or the untracked `CodebaseGuide/` cache. If evidence proves a boundary insufficient, stop and report rather than broadening scope.
4. Preserve direct trusted activation and capture every payload/ref/session token before its first `await`; prove no intermediate mixed session notification or cross-project write.
5. Implement every focused fixture and browser matrix in the RFC, including hostile UTF-16 IDs, ordinary-project dirty-copy deduplication, stable form identity/shortcut isolation, global-banner/action-error coexistence, exact breakpoint transitions, Hybrid Details evidence, DPR 1/2, rapid toggles, and real pointer interaction.
6. Before Playwright gates, explicitly stop the existing development server on port `5175`; strict-port refusal must remain enabled. Do not weaken `reuseExistingServer: false`.
7. Run the proportionate complete suite from `VisualSpecs`: focused unit tests during development, then core verification/typecheck/build, smoke/acceptance, architecture boundaries, performance evidence, and the dedicated screenshot update/review flow only if canonical screenshots intentionally change. Report exact commands and results.
8. Commit the implementation/tests/docs in reviewable issue-linked commit(s). Finish with a clean tracked tree and only the pre-existing untracked `CodebaseGuide/`.

Reply with a concrete completion or blocker report: commit SHA(s), changed-file summary, implementation notes for the atomic boundary and UI state machine, exact test counts/results, browser/performance/visual evidence, server cleanup, and tracked status. The implementation is not gate-final until core independently reviews it and both red teams falsify the executable diff.
