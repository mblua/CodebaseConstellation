# Issue #7 — own the Project Rail RFC/plan

You are the sole constructive owner of the graph/runtime UI artifact for `mblua/CodebaseConstellation#7`, **feat(visual-specs): add a collapsible project rail**.

Repository: `C:\Users\maria\0_repos\CodebaseConstellation_iac\.ac\wg-4-vs-dev-team\repo-CodebaseConstellation`

Branch: `feature/7-collapsible-project-rail`

Canonical issue and acceptance contract: https://github.com/mblua/CodebaseConstellation/issues/7

This is the **full** delivery path. For this first assignment, inspect the existing Visual Specs implementation and produce the shared RFC/implementation plan under `plan/` (prefer `plan/7-collapsible-project-rail.md` if compatible with repository convention). Cover the issue's before/requested/expected-after state, exact owned files or systems, mounted UI-state lifetime, focus/ARIA contract, renderer resize boundary, viewport/selection/layout preservation, responsive behavior at 1680×1000, 1024×768, and 800×800, focused tests, Playwright/visual evidence, rollback consequences, and explicit no-change boundaries for contracts/domain/projection/renderer port/filesystem/schema.

Record the product tradeoff and any transverse decisions needing constructive 2-of-3 support. Preserve the pre-existing untracked `CodebaseGuide/` cache and all unrelated changes. Do **not** implement production UI/runtime code yet: readiness requires constructive validation plus independent semantic and resilience premortems first.

Reply explicitly when the plan artifact is complete with: path, files changed, commit SHA if committed, key decisions/tradeoffs, verification performed, and any blocker or question that truly requires product input. Do not infer completion from repository state; send a concrete report.
